# Terraform
.terraform/
# .terraform.lock.hcl is intentionally tracked — pin provider versions
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars

# Ansible
*.retry
ansible/inventory/hosts.ini

# Secrets / env
.env
*.pem
*.key
