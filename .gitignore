# Terraform
.terraform/
# .terraform.lock.hcl is intentionally tracked — pin provider versions
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars

# Ansible
*.retry
ansible/inventory/hosts.ini
ansible/.vault_pass

# Packer
packer/*.pkrvars.hcl

# Secrets / env
.env
*.pem
*.key
